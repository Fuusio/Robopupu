package com.robopupu.api.feature;

import org.fuusio.api.feature.FeatureManager;
import org.fuusio.api.plugin.PlugInterface;

/**
 * {@link PluginFeatureManager} extends {@link FeatureManager} to provide a Plugi
 */
@PlugInterface
public interface PluginFeatureManager extends FeatureManager {
}
