package com.robopupu.api.component;

import com.robopupu.api.fsm.StateMachine;

/**
 * {@link GraphInteractor} extends {@link AbstractInteractor} to provide an abstract
 * base class for implementing {@link Interactor}s that utilise {@code Graph}s.
 */
public abstract class GraphInteractor extends AbstractInteractor {

    /*
    protected GraphInteractor() {
        createGraphs();
    }

    protected abstract void createGraphs();*/
}
